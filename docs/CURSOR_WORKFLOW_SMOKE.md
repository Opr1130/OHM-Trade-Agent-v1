# Cursor workflow smoke

This document exists solely to validate the local Cursor → Git → PR workflow.

It is developer-only. It does not change application behavior, trading authority, tests, CI, deployment, or configuration.
